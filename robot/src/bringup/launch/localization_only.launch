<launch>
    <!-- Phase 1: Nur Localization (GPS + NTRIP).
         roscore + rosbridge laufen bereits als systemd-Service (deploy/rosbridge.service).
         Wird manuell vom Frontend via start_localization.sh gestartet. -->
    <include file="$(find localization)/launch/localization.launch"/>
</launch>
